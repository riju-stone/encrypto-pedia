#include <iostream>
#include <vector>
#include <string>

using namespace std;

string to_number(string s) {}

string additive(string s, int k) {}

string subtractive(string s, int k) {}

string from_number(string s) {}